[package]
name = "lean-ffi-rs"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
lean-ffi = { path = ".." }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
